export enum AssetType {
    GOLD     = 'gold',
    STOCK    = 'stock',
    CURRENCY = 'currency',
  }
  
  export enum GoldUnit {
    GRAM  = 'gram',
    TOLA  = 'tola',
    OUNCE = 'ounce',
  }
  
  export enum Currency {
    PKR = 'PKR',
    USD = 'USD',
  }
  